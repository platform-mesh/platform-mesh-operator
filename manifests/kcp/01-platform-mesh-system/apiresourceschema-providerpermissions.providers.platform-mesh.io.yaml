apiVersion: apis.kcp.io/v1alpha1
kind: APIResourceSchema
metadata:
  name: v260624-bc73f71.providerpermissions.providers.platform-mesh.io
spec:
  group: providers.platform-mesh.io
  names:
    kind: ProviderPermissions
    listKind: ProviderPermissionsList
    plural: providerpermissions
    singular: providerpermissions
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      description: ProviderPermissions allows API providers to customize authorization
        for their resources.
      properties:
        apiVersion:
          description: |-
            APIVersion defines the versioned schema of this representation of an object.
            Servers should convert recognized schemas to the latest internal value, and
            may reject unrecognized values.
            More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
          type: string
        kind:
          description: |-
            Kind is a string value representing the REST resource this object represents.
            Servers may infer this from the endpoint the client submits requests to.
            Cannot be updated.
            In CamelCase.
            More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
          type: string
        metadata:
          type: object
        spec:
          description: ProviderPermissionsSpec defines the desired state of ProviderPermissions.
          properties:
            apiExportRef:
              description: APIExportRef references the APIExport this configuration
                applies to
              properties:
                name:
                  description: Name of the APIExport
                  type: string
              required:
              - name
              type: object
            permissions:
              additionalProperties:
                description: ResourcePermissions defines permissions for a specific
                  resource type.
                properties:
                  additionalPermissions:
                    additionalProperties:
                      type: string
                    description: AdditionalPermissions defines custom roles and custom
                      permissions
                    type: object
                  defaultPermissions:
                    description: |-
                      DefaultPermissions overrides standard Kubernetes verb permissions
                      Empty string "" means use system default
                    properties:
                      delete:
                        description: Delete permission expression (empty string uses
                          default)
                        type: string
                      get:
                        description: Get permission expression (empty string uses
                          default)
                        type: string
                      patch:
                        description: Patch permission expression (empty string uses
                          default)
                        type: string
                      update:
                        description: Update permission expression (empty string uses
                          default)
                        type: string
                      watch:
                        description: Watch permission expression (empty string uses
                          default)
                        type: string
                    type: object
                type: object
              description: |-
                Permissions defines per-resource permission configuration
                Key format: {resource}.{group} (e.g., "httpbin.orchestrate.platform-mesh.io")
              type: object
            roles:
              description: Roles defines custom roles grouped by resource type
              items:
                description: ResourceRoles defines roles for a specific resource type.
                properties:
                  groupResource:
                    description: |-
                      GroupResource identifies the resource type in format "{resource}.{group}"
                      (e.g., "httpbin.orchestrate.platform-mesh.io")
                    type: string
                  roles:
                    description: Roles defines the roles for this resource type
                    items:
                      description: RoleDefinition defines a custom role with UI metadata
                        and optional relation definition.
                      properties:
                        definition:
                          description: Definition is the OpenFGA relation definition
                            for this role
                          type: string
                        description:
                          description: Description explains the role's purpose
                          type: string
                        displayName:
                          description: DisplayName is the human-readable role name
                            for UIs
                          type: string
                        id:
                          description: ID is the role identifier (must match relation
                            name used in permissions)
                          type: string
                      required:
                      - description
                      - displayName
                      - id
                      type: object
                    type: array
                required:
                - groupResource
                - roles
                type: object
              type: array
          required:
          - apiExportRef
          type: object
        status:
          description: ProviderPermissionsStatus defines the observed state of ProviderPermissions.
          properties:
            conditions:
              description: Conditions represent the latest available observations
                of the object's state
              items:
                description: Condition contains details for one aspect of the current
                  state of this API Resource.
                properties:
                  lastTransitionTime:
                    description: |-
                      lastTransitionTime is the last time the condition transitioned from one status to another.
                      This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                    format: date-time
                    type: string
                  message:
                    description: |-
                      message is a human readable message indicating details about the transition.
                      This may be an empty string.
                    maxLength: 32768
                    type: string
                  observedGeneration:
                    description: |-
                      observedGeneration represents the .metadata.generation that the condition was set based upon.
                      For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                      with respect to the current state of the instance.
                    format: int64
                    minimum: 0
                    type: integer
                  reason:
                    description: |-
                      reason contains a programmatic identifier indicating the reason for the condition's last transition.
                      Producers of specific condition types may define expected values and meanings for this field,
                      and whether the values are considered a guaranteed API.
                      The value should be a CamelCase string.
                      This field may not be empty.
                    maxLength: 1024
                    minLength: 1
                    pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                    type: string
                  status:
                    description: status of the condition, one of True, False, Unknown.
                    enum:
                    - "True"
                    - "False"
                    - Unknown
                    type: string
                  type:
                    description: type of condition in CamelCase or in foo.example.com/CamelCase.
                    maxLength: 316
                    pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                    type: string
                required:
                - lastTransitionTime
                - message
                - reason
                - status
                - type
                type: object
              type: array
            observedGeneration:
              description: ObservedGeneration is the most recent generation observed
                for this object
              format: int64
              type: integer
          type: object
      type: object
    served: true
    storage: true
    subresources:
      status: {}
