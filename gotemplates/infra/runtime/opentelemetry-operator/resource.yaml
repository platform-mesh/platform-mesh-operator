{{- if .opentelemetryOperator.enabled }}
apiVersion: delivery.ocm.software/v1alpha1
kind: Resource
metadata:
  labels:
    artifact: chart
    repo: oci
  name: {{ .opentelemetryOperator.name }}
  namespace: {{ .helmReleaseNamespace }}
spec:
  componentRef:
    name: {{ .ocm.component.name }}
  ocmConfig:
    - apiVersion: delivery.ocm.software/v1alpha1
      kind: Repository
      name: {{ .ocm.repo.name }}
      namespace: {{ .releaseNamespace }}
  resource:
    byReference:
      referencePath:
      {{ range .ocm.referencePath }}
        - name: {{ .name }}
      {{ end }}
{{- if .opentelemetryOperator.absoluteReferencePath }}
      {{ range .opentelemetryOperator.absoluteReferencePath }}
        - name: {{ .name }}
      {{ end }}
{{- end }}
      resource:
        name: {{ .opentelemetryOperator.ocmResourceName }}
{{- end }}
